# 生产环境变量模板（提交到 git）
# 真实部署前复制为 .env.production 填入实际值

# admin 后端公网地址（必填，Cloudflare Workers 无法访问 localhost）
# 部署 admin 后推荐平台：Railway / Render / Fly.io
NEXT_PUBLIC_API_BASE=https://cuporacle-admin.example.com

# 强制使用 mock 数据（演示场景设为 1）
# NEXT_PUBLIC_USE_MOCK=1